
<% 
const collection = "footer";
const linkPrefix = systemConfig.prefixAdmin + `/${collection}/`;
const pathUpload =`backend/adminlte/images/${collection}/`;
%>

<% include ./../../helpers/sectionbox %>

<%- locals.flash %>
<% include ./../../elements/errors %>


<div class="card card-info card-outline">
<form action="<%= linkPrefix %>save/" method="POST" class="mb-0"  enctype="multipart/form-data">
    <div class="card-body">
        <div class="form-group row">
            <label for="copyright" class="col-sm-2 col-form-label text-sm-right required">Copyright</label>
            <div class="col-xs-12 col-sm-8">
                <input type="text" name="copyright" class="form-control form-control-sm"  value="<%= item.copyright %>" >
            </div>
        </div>
        <div class="form-group row">
            <label for="content" class="col-sm-2 col-form-label text-sm-right required">Content</label>
            <div class="col-xs-12 col-sm-8">
                <textarea id="id_content" name="content"><%= item.content %></textarea>
            </div>
        </div>
        <div class="form-group row">
            <label for="logoFooter" class="col-sm-2 col-form-label text-sm-right required">Logo Footer</label>
            <div class="col-xs-12 col-sm-8">
                <input type="file" name="logoFooter" class="form-control form-control-sm" >
            </div>
        </div>
        <% if(item.id !== undefined && item.id !== ''){ %>
        <div class="form-group row">
            <label for="logoFooter" class="col-sm-2 col-form-label text-sm-right required">Logo Footer</label>
            <img src="<%= pathUpload + item.logoFooter%>" alt="" style="height:50px;">
        </div>
        <% } %>
        
    
    </div>
    <div class="card-footer">
        <div class="col-12 col-sm-8 offset-sm-2">
            <input class="form-control" name="id" value="<%= item.id %>" type="hidden" >
            <input class="form-control" name="image_old" value="<%= item.logoFooter %>" type="hidden" >
            <button type="submit" class="btn btn-sm btn-success mr-1"> Submit</button>
            <a href="<%= linkPrefix %>" class="btn btn-sm btn-danger mr-1"> Cancel</a>
        </div>
    </div>
</form>
</div>